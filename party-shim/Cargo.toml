[package]
name = "party-shim"
version = "0.1.0"
edition = "2021"

[lib]
name = "PartyWin"
crate-type = ["cdylib"]

# Match the opt level the legacy build.ps1 used (rustc -C opt-level=2).
[profile.release]
opt-level = 2
